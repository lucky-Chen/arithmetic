#include "MyHeap.h"

template <typename T>
MyHeap<typename T>::MyHeap()
{
	length = -1;
}

template <typename T>
int MyHeap<typename T>::getLength()
{
	return length;
}
template <typename T>
T *MyHeap<typename T>::makeMinHeap(T *dataList, int length)
{

}
template <typename T>
void MyHeap<typename T>::MinHeapFixup(T *dataList, int i)
{
	return NULL;
}
template <typename T>
void MyHeap<typename T>::MinHeapFixdown(T *dataList, int i)
{

}
template <typename T>
void MyHeap<typename T>::addMinHeapData(T *dataList, int length, T data)
{

}
template <typename T>
void MyHeap<typename T>::delMinHeapData(T *dataList, int length)
{

}